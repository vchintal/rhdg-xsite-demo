kind: Service
apiVersion: v1
metadata:
  name: session-remote-store-nlb
  namespace: {{ common_namespace }}
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    application: session-cache
  type: LoadBalancer 
